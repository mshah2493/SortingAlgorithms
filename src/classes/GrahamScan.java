package classes;

public class GrahamScan 
{
	public static void ConvexHull(Comparable[] a)
	{
		/*
		Stack<Point2D> hull = new Stack<Point2D>();
		
		Arrays.sort(points, Point2D.Y_ORDER);
		Arrays.sort(points, points[0].BY_POLAR_ORDER);
		
		hull.push(points[0]);
		hull.push(points[1]);
		
		for (int i = 2; i < N; i++)
		{
			Point2D top = hull.pop();
			
			while(Point2D.IsCounterClockWise(hull.peek(), top, points[i]) <= 0)
			{
				top = hull.pop();
			}
			
			hull.push(top);
			hull.push(points[i]);
		}
		*/
	}
}
